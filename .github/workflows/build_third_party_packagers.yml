name: Check Third-Party Packager Compatibility

on:
  push:
    branches: [release]
  pull_request:
    branches: [master, release]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' && github.ref != 'refs/heads/release' }}

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.13']
        os: [windows-latest, ubuntu-latest, macos-15-intel, macos-latest]
        packager: [pyinstaller, cxfreeze, nuitka]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        enable-cache: true
        cache-dependency-glob: "pyproject.toml"

    - name: Install just
      uses: extractions/setup-just@v2

    - name: Test ${{ matrix.packager }} (Windows)
      if: runner.os == 'Windows'
      shell: bash
      run: |
        export SHELL="$(which bash)"
        just test-${{ matrix.packager }}

    - name: Test ${{ matrix.packager }} (Unix)
      if: runner.os != 'Windows'
      run: |
        just test-${{ matrix.packager }}
